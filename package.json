{
  "name": "PopReplay",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm --parallel --filter=server dev & pnpm --filter=web dev",
    "start": "pnpm --parallel --filter=server start & pnpm --filter=web dev",
    "server": "pnpm --filter=server start",
    "web": "pnpm --filter=web dev",
    "test": "CI=1 pnpm -r --if-present run test",
    "test:parallel": "CI=1 pnpm -r --parallel --if-present run test",
    "test:web:watch": "pnpm --filter=web run test:watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "zod": "^3.25.67"
  }
}
